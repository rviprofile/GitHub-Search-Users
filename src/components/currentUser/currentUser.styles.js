import styled from 'styled-components';

export const Block = styled.div`
    position: fixed;
    z-index: 1;
    width: calc(120px + (100vw / 3));
    height: 700px;
    background-color: #c8d4e6;
    border-radius: 30px;
    margin-top: 50px;
    box-shadow: 4px 4px 25px 0px rgba(0, 0, 0, 0.38);
`;
